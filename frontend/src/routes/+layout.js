// Desabilita SSR globalmente (SPA mode)
export const ssr = false;

// Habilita client-side rendering
export const csr = true;

// Opcional: pré-renderizar a página inicial
export const prerender = false;
